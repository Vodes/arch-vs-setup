[arch-enc]
image=arch-vs:latest
init=true
nvidia=true # Change this in case you don't have nvidia ofc (this requires proper drivers on the host)
home=~/enc-home

init_hooks="curl -fsSL https://raw.githubusercontent.com/Vodes/arch-vs-setup/master/init-hooks/1.sh | bash"
